<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Storybook</title>
</head>
<body>
    {{#each nav}}
        <strong>{{name}}</strong><br>
        <ul>
            {{#each items}}
            <li>{{ title }}</li>
            {{/each}}
        </ul>
    {{/each}}
    <hr>
	<h1>{{ title }}</h1>
	<div>{{{ contents }}}</div>
</body>
</html>